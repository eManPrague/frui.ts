{{#*inline "type"~}}
{{#if hasTypeImport}}{{ typeName }}{{else}}z.{{ typeName }}(){{/if}}
{{~/inline~}}

function build{{name}}() {
  return z.union([
  {{#each subEntities}}
    {{> type}},
  {{/each}}
  ]);
}