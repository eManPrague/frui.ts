{{#each imports}}
{{{this}}}
{{/each}}
import z from "zod";

{{> generatedEntityHeader}}
function build{{pascalCase entity.name}}() {
  {{{content}}}
}

export const {{pascalCase entity.name}} = build{{pascalCase entity.name}}();
export type {{pascalCase entity.name}} = z.infer<typeof {{pascalCase entity.name}}>;
