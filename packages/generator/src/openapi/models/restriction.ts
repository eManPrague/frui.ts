// eslint-disable-next-line @typescript-eslint/no-unused-vars
enum Restriction {
  required,
  number,
  maxLength,
  minLength,
  pattern,
  nullable,
  readOnly,
  format,
}
export default Restriction;
