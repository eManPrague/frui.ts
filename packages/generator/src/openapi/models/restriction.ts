// eslint-disable-next-line @typescript-eslint/no-unused-vars
enum Restriction {
  required,
  number,
  date,
  maxLength,
  minLength,
  pattern,
  nullable,
  readOnly,
  format,
}
export default Restriction;
